name: Build and Push Docker Image

on:
  push:
    branches:
      - main  # Ejecuta el flujo al hacer push en la rama principal
  workflow_dispatch:  # Permite ejecución manual

permissions:
  contents: read
  packages: write

jobs:
  build-and-push:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Log in to GitHub Container Registry
        run: echo "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" | docker login ghcr.io -u dyagus1329@gmail.com --password-stdin

      - name: Build Docker image
        run: |
          docker build -t ghcr.io/${{ github.repository_owner }}/auth-service:latest .

      - name: Push Docker image to GHCR
        run: |
          docker push ghcr.io/${{ github.repository_owner }}/auth-service:latest
